<Window
    x:Class="Frost.Views.PlaytimeWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="Krew Watching">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid x:Name="RootGrid" Padding="0">
        <Grid.RowDefinitions>
            <!-- Custom draggable title bar -->
            <RowDefinition Height="32"/>
            <!-- Content -->
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Transparent title bar area -->
        <Grid x:Name="AppTitleBar" Background="Transparent" Grid.Row="0"/>

        <!-- Main content -->
        <Grid Grid.Row="1" Padding="10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="100"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Image Source="{Binding CoverImgUrl}" 
                   Width="80" Height="110" Stretch="UniformToFill" />

            <StackPanel Grid.Column="1" Margin="10,0,0,0" VerticalAlignment="Center">
                <TextBlock Text="{Binding Name}" FontWeight="Bold" FontSize="18"/>
                <TextBlock Text="{Binding Company}" FontSize="14" Margin="0,5,0,0"/>
                <TextBlock x:Name="PlaytimeText" Text="00:00:00" 
                           FontSize="16" Foreground="DodgerBlue" Margin="0,10,0,0"/>
            </StackPanel>
        </Grid>
    </Grid>
</Window>
